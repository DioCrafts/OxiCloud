use std::collections::HashMap;
use lazy_static::lazy_static;

lazy_static! {
    pub static ref TRANSLATIONS: HashMap<&'static str, &'static str> = {
        let mut m = HashMap::new();
        m.insert("Failed to clear the mappings.", "清除映射失敗");
        m.insert("Failed to delete the server configuration", "刪除伺服器設定時失敗");
        m.insert("The configuration is valid and the connection could be established!", "設定有效且連線可建立");
        m.insert("The configuration is valid, but the Bind failed. Please check the server settings and credentials.", "設定有效但連線無法建立，請檢查伺服器設定與認證資料。");
        m.insert("Deletion failed", "移除失敗");
        m.insert("Take over settings from recent server configuration?", "要使用最近一次的伺服器設定嗎？");
        m.insert("Keep settings?", "維持設定嗎？");
        m.insert("Cannot add server configuration", "無法新增伺服器設定");
        m.insert("mappings cleared", "映射已清除");
        m.insert("Success", "成功");
        m.insert("Error", "錯誤");
        m.insert("Select groups", "選擇群組");
        m.insert("Connection test succeeded", "連線測試成功");
        m.insert("Connection test failed", "連線測試失敗");
        m.insert("Do you really want to delete the current Server Configuration?", "您真的要刪除現在的伺服器設定嗎？");
        m.insert("Confirm Deletion", "確認刪除");
        m.insert("_%s group found_::_%s groups found_", "");
        m.insert("_%s user found_::_%s users found_", "");
        m.insert("Save", "儲存");
        m.insert("Test Configuration", "測試此設定");
        m.insert("Help", "說明");
        m.insert("Add Server Configuration", "新增伺服器設定");
        m.insert("Host", "主機");
        m.insert("You can omit the protocol, except you require SSL. Then start with ldaps://", "若您不需要 SSL 加密連線則不需輸入通訊協定，反之請輸入 ldaps://");
        m.insert("Port", "連接埠");
        m.insert("User DN", "User DN");
        m.insert("The DN of the client user with which the bind shall be done, e.g. uid=agent,dc=example,dc=com. For anonymous access, leave DN and Password empty.", "客戶端使用者的DN與特定字詞的連結需要完善，例如:uid=agent,dc=example,dc=com。若是匿名連接，則將DN與密碼欄位留白。");
        m.insert("Password", "密碼");
        m.insert("For anonymous access, leave DN and Password empty.", "匿名連接時請將 DN 與密碼欄位留白");
        m.insert("One Base DN per line", "一行一個 Base DN");
        m.insert("You can specify Base DN for users and groups in the Advanced tab", "您可以在進階標籤頁裡面指定使用者及群組的 Base DN");
        m.insert("Back", "返回");
        m.insert("Continue", "繼續");
        m.insert("<b>Warning:</b> The PHP LDAP module is not installed, the backend will not work. Please ask your system administrator to install it.", "<b>警告：</b>沒有安裝 PHP LDAP 模組，後端系統將無法運作，請要求您的系統管理員安裝模組。");
        m.insert("Connection Settings", "連線設定");
        m.insert("Configuration Active", "設定使用中");
        m.insert("When unchecked, this configuration will be skipped.", "沒有被勾選時，此設定會被略過。");
        m.insert("User Login Filter", "User Login Filter");
        m.insert("Backup (Replica) Host", "備用主機");
        m.insert("Give an optional backup host. It must be a replica of the main LDAP/AD server.", "可以選擇性設定備用主機，必須是 LDAP/AD 中央伺服器的複本。");
        m.insert("Backup (Replica) Port", "備用（複本）連接埠");
        m.insert("Disable Main Server", "停用主伺服器");
        m.insert("Case insensitve LDAP server (Windows)", "不區分大小寫的 LDAP 伺服器 (Windows)");
        m.insert("Turn off SSL certificate validation.", "關閉 SSL 憑證檢查");
        m.insert("Cache Time-To-Live", "快取的存活時間");
        m.insert("in seconds. A change empties the cache.", "以秒為單位。變更後會清空快取。");
        m.insert("Directory Settings", "目錄設定");
        m.insert("User Display Name Field", "使用者顯示名稱欄位");
        m.insert("Base User Tree", "Base User Tree");
        m.insert("One User Base DN per line", "一行一個使用者 Base DN");
        m.insert("User Search Attributes", "User Search Attributes");
        m.insert("Optional; one attribute per line", "非必要，一行一項屬性");
        m.insert("Group Display Name Field", "群組顯示名稱欄位");
        m.insert("Base Group Tree", "Base Group Tree");
        m.insert("One Group Base DN per line", "一行一個 Group Base DN");
        m.insert("Group Search Attributes", "Group Search Attributes");
        m.insert("Group-Member association", "Group-Member association");
        m.insert("Special Attributes", "特殊屬性");
        m.insert("Quota Field", "配額欄位");
        m.insert("Quota Default", "預設配額");
        m.insert("in bytes", "以位元組為單位");
        m.insert("Email Field", "電郵欄位");
        m.insert("User Home Folder Naming Rule", "使用者家目錄的命名規則");
        m.insert("Leave empty for user name (default). Otherwise, specify an LDAP/AD attribute.", "使用者名稱請留白(預設)。若不留白請指定一個LDAP/AD屬性。");
        m.insert("Internal Username", "內部使用者名稱");
        m
    };
    
    pub static ref PLURAL_FORMS: &'static str = "nplurals=1; plural=0;";
}